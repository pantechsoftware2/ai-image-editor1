═══════════════════════════════════════════════════════════════
  GOOGLE CLOUD AUTHENTICATION - STEP BY STEP FIX
═══════════════════════════════════════════════════════════════

STATUS: Google Cloud SDK NOT installed on your system

WHAT YOU NEED TO DO (3 simple steps - 10 minutes total):
═══════════════════════════════════════════════════════════════

STEP 1: INSTALL GOOGLE CLOUD SDK
─────────────────────────────────────────────────────────────

Method A - Online Installer (Easiest):

1. Open your web browser
2. Go to: https://cloud.google.com/sdk/docs/install-gcloud
3. Click "Windows 64-bit (x86_64) Installer"
4. Download GoogleCloudSDKInstaller.exe
5. Double-click the .exe file
6. Follow the installer steps (just click "Next" on everything)
7. When asked to "Initialize the Cloud SDK":
   → Choose: YES, initialize Cloud SDK
   → When browser opens, sign in with your Google account
   → Accept permissions

After installation:
- Installer finishes
- You may see a terminal window - that's normal
- Close all installer windows

═══════════════════════════════════════════════════════════════

STEP 2: RESTART POWERSHELL (IMPORTANT!)
─────────────────────────────────────────────────────────────

After installation:
1. Close ALL PowerShell windows completely
2. Open a NEW PowerShell window
3. Try typing: gcloud --version
   → Should show: Google Cloud SDK [version] ...

═══════════════════════════════════════════════════════════════

STEP 3: AUTHENTICATE WITH GOOGLE
─────────────────────────────────────────────────────────────

In the NEW PowerShell window, run:

  gcloud auth application-default login

What happens next:
1. Your default web browser opens
2. Google login page appears
3. Sign in with your Google account
4. A permissions page appears - click "ALLOW"
5. You should see: "You are now authenticated with the Google Cloud CLI!"
6. PowerShell shows: "Credentials saved to: [path]"

═══════════════════════════════════════════════════════════════

STEP 4: VERIFY IT WORKED
─────────────────────────────────────────────────────────────

In PowerShell, run:

  gcloud auth application-default print-access-token

Expected result:
  Should print a long string of random characters (your token)
  
If it worked:
  ✅ You're authenticated! Move to STEP 5
  
If error:
  ❌ Something wrong, try Step 3 again

═══════════════════════════════════════════════════════════════

STEP 5: RESTART THE DEV SERVER
─────────────────────────────────────────────────────────────

In PowerShell, navigate to the project:

  cd "C:\Users\pante\Downloads\ai-image-editor"
  
Stop any running npm (press Ctrl+C if it's running)

Then start fresh:

  npm run dev

Expected output:
  ✅ "Ready in X.XXs"
  ✅ "Ready on http://localhost:3000"

═══════════════════════════════════════════════════════════════

STEP 6: TEST IMAGE GENERATION
─────────────────────────────────────────────────────────────

1. Go to: http://localhost:3000
2. Login with your account
3. Click "Editor" or "Go to Editor"
4. In the URL field, type: google.com
5. Click "✨ Extract Brand DNA"
6. Wait for brand to extract
7. Click "Confirm" to accept brand colors
8. In the "Generate Image" field, type: coffee
9. Click "🎨 Generate Image"
10. WAIT 30-60 SECONDS... (it's generating 4 images)
11. You should see the 4 image variants appear in a grid!

═══════════════════════════════════════════════════════════════

WHAT IF IT STILL DOESN'T WORK?
─────────────────────────────────────────────────────────────

Problem: Still seeing "Unable to authenticate"
→ You may not have authenticated properly
→ Try Step 3 again: gcloud auth application-default login

Problem: "gcloud: command not found"
→ Restart PowerShell (close completely and reopen)
→ Or check if installation completed successfully

Problem: Authentication works but images still won't generate
→ Open browser DevTools (F12)
→ Go to "Console" tab
→ Try generating image again
→ Look for error messages in console
→ Send me the error message

═══════════════════════════════════════════════════════════════

IMPORTANT NOTES:
─────────────────────────────────────────────────────────────

✓ Your .env.local already has GOOGLE_CLOUD_PROJECT_ID set
✓ You just need to authenticate once
✓ Credentials are saved on your computer
✓ You won't need to do this again

═══════════════════════════════════════════════════════════════

READY? HERE'S THE QUICK CHECKLIST:

☐ Download & run GoogleCloudSDKInstaller.exe
☐ Restart PowerShell  
☐ Run: gcloud auth application-default login
☐ Sign in when browser opens
☐ Run: gcloud auth application-default print-access-token (verify it works)
☐ Run: npm run dev
☐ Test image generation with "coffee" prompt

═══════════════════════════════════════════════════════════════

Your Project Setup:
  Project ID: ai-image-editor-483505
  Region: us-central1
  Model: Imagen-4.0-fast-generate
  
Ready to go! Just need to install and authenticate. 🚀
